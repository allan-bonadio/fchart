// stuff I'm too chicken to hardwire

const config = {
	editorWidth: 700,  // size of flowchart + tilebar on left
	editorHeight: 400,
	
	tileBarWidth: 100,
	
	// tiles
	tileWidth: 80,
	tileHeight: 40,
	tileMaxInlets: 3,
	
	// arrows.  The direction can be any 360.
}

export default config;
